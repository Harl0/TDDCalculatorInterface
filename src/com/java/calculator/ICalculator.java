package com.java.calculator;

//Declaring interface
public interface ICalculator {

	// Step 1 is to create the means of adding two numbers together
	int sum(int a, int b);
	
	// Step 2 is to create the means of multiplying two numbers together
	int multiplication(int a, int b);
}
